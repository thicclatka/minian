# After clone: uv sync --group dev && uv run pre-commit install
repos:
  - repo: https://github.com/psf/black
    rev: "24.10.0"
    hooks:
      - id: black
        language_version: python3.12
        files: ^minian/
